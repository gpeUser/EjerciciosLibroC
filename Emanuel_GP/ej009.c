/**********************************************************************
 *                          Grupo de Estudio C
 * Autor:          Emanuel GP
 * Libro:          Introduccion a la programacion con C
 * Semana:         1
 * Tema:           [I]: Introduccion a C
 * Ejercicio:      9
 **********************************************************************/

#include <stdio.h> // cabecera de entrada/salida de datos estandar bien definida.

int a, b; // variables enteras bien definidas.
/* 
 * No esta declarada/definida la funcion principal(main).
 * */
scanf("%d", &a); scanf("%d", &b) // primer sentencia(scanf) bien definida, la segunda sentencia carece del punto y coma(;) de finalizacion de sentencia.
while (a <= b):
/*
 * bucle(while) mal definido y semiestructurado, no debe terminar la sentencia de declaracion con un signo de dos puntos(:),
 * y no tiene los parentesis delimitadores[(,)] que permiten definir explicitamente el bloque de sentencias del bucle.
 * */
   scanf("%d", &a) // sentencia mal definidia, carece del punto y coma(;) de finalizacion de la sentencia.
   scanf("%d", &b) // sentencia mal definidia, carece del punto y coma(;) de finalizacion de la sentencia.
printf("%d %d\n", a, b);
